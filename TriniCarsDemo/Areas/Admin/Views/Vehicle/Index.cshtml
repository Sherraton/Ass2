@model List<Vehicle>

<div class="card shadow border-0 my-4 ">
    <div class="card-header bg-secondary bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2"> Vehicle List</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4" style="100%">
        <div class="row pb-3">
            <div class="col-6">
            </div>
            <div class="col-6 text-end">
                <a asp-controller="Vehicle" asp-action="Upsert" class="btn btn-primary">
                    <i class="bi bi-plus-circle"></i> Create New Vehicle
                </a>
            </div>
        </div>
        <table id="tblData" class="table table-bordered table-striped" style="100%">
            <thead>
                <tr>
                    <th>Make</th>
                    <th>Model</th>
                    <th>License#</th>
                    <th>Year</th>
                    <th>Engine Size</th>
                    <th>Transmission</th>
                    <th>Features</th>
                    <th>Additional Info</th>
                    <th>Color</th>
                    <th>Mileage</th>
                    <th>Asking Price</th>
                    <th>Category</th>
                    <th></th>
                </tr>
            </thead>
            @* <tbody>
                @foreach (var obj in Model)
                {
                    <tr>
                        <td>@obj.Make</td>
                        <td>@obj.Model</td>
                        <td>@obj.LicenseNo</td>
                        <td>@obj.Year</td>
                        <td>@obj.EngineSize</td>
                        <td>@obj.Transmission</td>
                        <td>@obj.AskingPrice</td>
                        <td>@obj.Category.Name</td>
                        <td>
                            <div class="w-75 btn-group" role="group">
                                <a asp-controller="Vehicle" asp-action="Upsert" asp-route-id="@obj.Id" class="btn btn-primary mx-2">
                                    <i class="bi bi-pencil-square"></i> Edit
                                </a>
                                <a asp-controller="Vehicle" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-2">
                                    <i class="bi bi-trash-fill"></i> Delete
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody> *@
        </table>
    </div>
</div>

@section Scripts{
    <script src="~/js/vehicle.js"></script>
}